name: vector_lane
type: compute
source: vector_lane.loom
resources:
- name: vector_pipeline
- name: vector_lane
performance:
  vector_add:
  - constraint: (L > 0) && (L <= 1024)
    latency: '2'
    volume: L
    throughput: '32'
  - constraint: (L > 0) && (L > 1024)
    latency: '18'
    volume: L
    throughput: '64'
