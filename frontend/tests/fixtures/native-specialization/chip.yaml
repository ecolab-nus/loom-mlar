name: native_specialization
memory: memory.yaml
memories:
  S: {domain: L1}
  R: {domain: L1}
processors:
  lane_sr:
    definition: lane.yaml
    inputs: {lhs: S, rhs: R}
    outputs: {result: S}
  lane_rs:
    definition: lane.yaml
    inputs: {lhs: R, rhs: S}
    outputs: {result: S}
