# Implementation Plan: Memory-Centric Visualization

> **Historical record.** This feature is implemented and closed. Canonical documentation lives in `docs/` and `README.md`; this directory is retained for decision traceability only.

**Branch**: `001-memory-centric-visualization` | **Date**: 2026-08-17 | **Spec**: [spec.md](spec.md)

**Input**: Feature specification from `specs/001-memory-centric-visualization/spec.md`

## Summary

Refactor the generated architecture explorer around two concepts: a default `System View` and focused `Component Views`. Retain the existing `mlar.visualization.v1` document and Rust exporter: the current contract already contains stable component IDs, scope hierarchy and replication, recursive array/bank details, directional read/write relationships, resource requirements, and network attachments. `System View` combines memory hierarchy, actors, routes, and scope boundaries when it fits. Every memory, processor, and data mover also receives a one-hop component view containing the anchor, all directly connected canonical components, and every relationship between them. Resources and networks appear as direct neighbors rather than standalone focus views; otherwise uncovered entities are grouped by owning architecture scope. Preserve the 12-node bound, canonical source IDs, exact relationship semantics, complete coverage, English-only output, and Archify as the sole renderer.

## Technical Context

**Language/Version**: Rust edition 2024 for the library projection; JavaScript ES modules on Node.js 20 or newer for the visualization adapter and generated gallery

**Primary Dependencies**: Rust `serde` and `serde_yaml`; adapter `ajv` 8 and `yaml` 2; vendored project-relative Archify 2.14 renderer

**Storage**: Human-readable versioned YAML input, generated Archify JSON specifications, standalone HTML diagrams, bundle manifest, and conversion report; no database or server-side state

**Testing**: Rust unit and integration tests through `cargo test`; Node built-in test runner through `npm test --prefix tools/mlar-archify`; vendored Archify showcase validation, delivery, and visual checking; Docusaurus typecheck/build for documentation changes

**Target Platform**: Rust library consumers on stable-toolchain platforms; static browser application generated by Node.js 20+ and deployable without a backend

**Project Type**: Library plus repository-owned static visualization adapter and documentation site

**Performance Goals**: Deterministic planning; no more than 12 primary nodes in any semantic diagram; no expansion proportional to replicated instance count; 100% source scope/component/relationship coverage; representative users can identify memory access within 60 seconds

**Constraints**: Preserve `mlar.visualization.v1`; do not change architecture, schedule, or MLIR semantics; never infer access from scope hierarchy; retain distinct canonical entities; keep replication as metadata; use Archify as the only renderer; English-only authored output; report unavailable browser validation as skipped

**Scale/Scope**: Multi-level and replicated hardware models, using the 2D mesh fixture as the primary acceptance case (2 scopes, 17 components, 22 relationships); its 9 memory/layer/actor nodes fit one primary diagram, while larger models are partitioned rather than expanded or collapsed

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-checked after Phase 1 design.*

| Principle / Gate | Pre-Research | Post-Design | Plan Evidence |
| --- | --- | --- | --- |
| I. Preserve library contracts and model semantics | PASS | PASS | Keep the Rust public API, architecture model, schedule behavior, MLIR path, and `mlar.visualization.v1` payload unchanged. The refactor consumes existing scope, region, and relationship data in the adapter. |
| II. Verify with proportionate evidence | PASS | PASS | Add focused planner/gallery tests, run existing Rust visualization regressions, build the representative bundle, inspect coverage and receipts, and run documentation checks where files change. |
| III. Complete and bounded visualization semantics | PASS | PASS | Preserve every source ID in at least one view, enforce 12 primary nodes, repeat canonical anchors without collapsing them, retain replication metadata, and keep all authored labels English. |
| IV. Structural documentation is visual and reproducible | PASS | PASS | Update canonical visualization documentation. If maintained Archify JSON descriptions change, validate, deliver, and visually check their generated HTML before the docsite build. Consolidation into canonical docs is part of feature completion. |
| V. Protect source truth and user work | PASS | PASS | The plan isolates changes to adapter behavior, tests, and docs; requires fixture-diff inspection and final worktree review; and forbids patching generated HTML. |
| VI. Apply Occam's Razor | PASS | PASS | Reuse the current versioned payload and add one internal memory projection instead of introducing a second hierarchy source or schema version. |
| VII. Human-readable textual contracts | PASS | PASS | YAML remains the canonical renderer-neutral interchange; generated JSON reports/specifications remain inspectable; no opaque in-memory-only boundary is introduced. |

No constitution violations require justification.

## Technical Design

### 1. Normalize Memory Presentation Data in the Adapter

- Index scopes by ID and derive each scope's stable root-to-leaf path from `parent_scope`.
- Treat each top-level memory component as the only canonical access endpoint. Keep its source ID unchanged in every view.
- Walk each memory component's recursive `region` value to create presentation-only array/bank layer nodes. Derive their IDs from the canonical memory ID plus structural child path, never from display names.
- Mark derived layers as structural detail rather than canonical access targets. Read/write edges continue to terminate only at canonical memory components.
- Format dimensions, replication, capacity, block size, total size, symbolic expression text, and unconnected status from existing fields.

### 2. Replace Scope-First Planning with System View

- Build one `system_view` diagram containing all canonical memories, recursive layer nodes, directly connected processors/data movers, derived `contains` connections, and original read/write relationships when the union contains at most 12 nodes.
- Place canonical memories in hierarchy-depth columns, their recursive layers directly below them, and connected actors in the intervening column between their exact source and destination memories whenever those endpoints differ.
- Show scope ownership with boundaries. Scope containment communicates placement only and MUST NOT create memory access edges.
- Deduplicate each actor while including every directly connected canonical memory endpoint and every original directional relationship. Render those relationships as arrow-only source-memory → actor → destination-memory routes, without `read` or `write` text. Same-memory source/destination pairs remain two directional segments around one actor identity.
- Only when the union exceeds 12 nodes, fall back to deterministic bounded System View windows. Exact routes remain available in the always-generated Component Views. Repeat stable context without aggregate placeholders or instance expansion.

### 3. Generate Exact One-Hop Component Views

- Create a focused `component_views` entry for every canonical memory, processor, and data mover, including anchor-only views for unconnected components.
- Include only relationships incident to the anchor and their opposite canonical endpoints. Do not add transitive neighbors or infer relationships from shared scope, names, hierarchy, or a neighbor's dependencies.
- Keep all relationships to the same neighbor together, repeat the stable anchor across deterministic overflow pages, and cap each page at 12 primary nodes.
- Show resources required by a processor/data mover and networks attached to a memory as direct neighbors. Do not create resource- or network-focused views by default.
- Group otherwise uncovered components and empty scopes in fallback entries explicitly named for their owning `Architecture Scope`.
- Keep source relationship IDs separate from derived containment relationships so the conversion report continues to prove exact source coverage.
- Fail generation when any source scope, component, or relationship is omitted or when references are invalid.

### 4. Make Gallery Navigation Component-Centric

- Expose exactly two reader-facing sections: `System View`, then `Component Views`.
- Default to the root `System View`.
- Add focus-component ID, name, kind, canonical memory metadata, and scope paths to catalog search; retain scope filtering, URL hashes, keyboard navigation, independent opening, and iframe embedding.
- Override the architecture renderer's present component-kind legend labels so the primary diagram reads `Memory`, `Processor`, and `Data Mover`. Explain in the diagram subtitle that arrows are processor/data-mover input/output paths and boundaries are architecture scopes.

### 5. Compatibility and Documentation

- Accept every currently valid `mlar.visualization.v1` document without migration.
- Do not modify `src/visualization/document.rs`, `src/lib.rs`, the v1 schema, or the tracked YAML unless implementation discovers a concrete missing datum. Any such discovery stops implementation for a compatibility decision rather than silently mutating v1.
- Update canonical user documentation to describe `System View`, exact one-hop `Component Views`, overflow partitioning, and MLAR legend labels.
- Update maintained Archify documentation sources only where their planner descriptions become inaccurate; use the mandated validate/deliver/visual-check workflow for each changed source.

## Project Structure

### Documentation (this feature)

```text
specs/001-memory-centric-visualization/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   ├── visualization-input.md
│   └── memory-centric-bundle.md
└── tasks.md                 # Created later by $speckit-tasks
```

### Source Code (repository root)

```text
src/
└── visualization/
    └── document.rs          # Existing v1 projection; regression boundary, no planned change

schemas/
└── mlar-visualization-v1.schema.json  # Existing input contract; no planned change

tools/mlar-archify/
├── bin/
│   └── mlar-archify.mjs     # Memory projection, bounded view planning, coverage
├── lib/
│   └── gallery.mjs          # Memory-first catalog and static navigation shell
├── test/
│   └── planner.test.mjs     # Unified view, overflow, route, coverage, gallery tests
└── README.md

tests/
├── visualization_export_test.rs
└── 2d_mesh/
    ├── arch.rs
    ├── tests.rs
    └── 2d_mesh_torus.visualization.yaml

docs/
├── architecture-concepts.md
├── installation.md
├── project-overview.md
├── software-architecture.md
├── usage.md
├── mlar-project-architecture.json  # Update only if its description becomes inaccurate
└── project-overview.json           # Update only if its description becomes inaccurate

README.md
docsite/
└── ...                       # Typecheck/build validation for changed canonical docs
```

**Structure Decision**: Keep the established split between renderer-neutral Rust projection, strict textual schema, project-owned Node adapter/gallery, and vendored Archify renderer. The feature is an adapter planning and navigation refactor; it does not introduce a new application, service, renderer, or public library abstraction.

## Feature Record Disposition

Retain `specs/001-memory-centric-visualization/` as the historical feature
record. The durable user and maintainer behavior has been consolidated into the
canonical README and `docs/` pages, while this directory preserves the approved
requirements, compatibility decision, acceptance evidence, and task history.
Archiving it would remove useful rationale without reducing any runtime or
maintenance mechanism.
