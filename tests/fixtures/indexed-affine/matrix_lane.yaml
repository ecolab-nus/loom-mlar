name: matrix_lane
type: compute
source: matrix_lane.loom
resources:
- name: matrix
performance:
  matmul_f16:
  - constraint: M > 0 && N > 0 && K > 0
    latency: '4'
    volume: 2 * M * N * K
    throughput: '512'
