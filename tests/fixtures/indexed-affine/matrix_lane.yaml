name: matrix_lane
type: compute
source: matrix_lane.loom
resources:
  - name: matrix
performance:
  functions:
    matmul_f16:
      constraints: "M > 0 && N > 0 && K > 0"
      time_cost:
        simple:
          fixed_latency: "4"
          volume: "2 * M * N * K"
          throughput: "512"
